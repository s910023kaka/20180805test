@page
@model _20180805.Pages.Servers.IndexModel

@{
    ViewData["Title"] = "Index";
}

<h2>Index</h2>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Server[0].Name)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Server[0].Location)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Server[0].Owner)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.Server) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Name)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Location)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Owner)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.ServerID">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.ServerID">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.ServerID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
